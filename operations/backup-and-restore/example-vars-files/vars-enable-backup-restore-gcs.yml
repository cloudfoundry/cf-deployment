droplet_backup_directory_key:  # Name of the backup bucket for droplets
app_package_backup_directory_key:  # Name of the backup bucket for app packages
buildpack_backup_directory_key:  # Name of the backup bucket for buildpacks
