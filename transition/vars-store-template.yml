---
blobstore_admin_users_password: (( merge ))
blobstore_admin_users_username: (( merge ))
blobstore_secure_link_secret: (( merge ))
cc_bridge_cc_uploader:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
cc_bridge_tps:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
cc_bulk_api_password: (( merge ))
cc_database_password: (( merge ))
cc_database_username: (( merge ))
consul_encrypt_key: (( merge ))
system_domain: (( merge ))
uaa_scim_users_admin_password: (( merge ))
cc_db_encryption_key: (( merge ))
cc_internal_api_password: (( merge ))
cc_staging_upload_password: (( merge ))
cc_tls:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
consul_agent:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
consul_agent_ca:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
consul_server:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
diego_auctioneer_client:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
diego_auctioneer_server:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
diego_bbs_client:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
diego_bbs_active_key_label: (( merge ))
diego_bbs_encryption_keys_passphrase: (( merge ))
diego_bbs_server:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
diego_database_password: (( merge ))
diego_rep_agent:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
diego_rep_client:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
diego_ssh_proxy_host_key:
  private_key: (( merge ))
  public_key_fingerprint: (( merge ))
dropsonde_shared_secret: (( merge ))
etcd_ca:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
etcd_client:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
etcd_peer:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
etcd_peer_ca:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
etcd_server:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
loggregator_ca:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
loggregator_tls_doppler:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
loggregator_tls_metron:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
loggregator_tls_statsdinjector:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
loggregator_tls_syslogdrainbinder:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
loggregator_tls_tc:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
nats_password: (( merge ))
router_route_services_secret: (( merge ))
router_status_password: (( merge ))
service_cf_internal_ca:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
uaa_admin_client_secret: (( merge ))
uaa_ca:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
uaa_clients_cc-routing_secret: (( merge ))
uaa_clients_cc-service-dashboards_secret: (( merge ))
uaa_clients_cloud_controller_username_lookup_secret: (( merge ))
uaa_clients_doppler_secret: (( merge ))
uaa_clients_gorouter_secret: (( merge ))
uaa_clients_ssh-proxy_secret: (( merge ))
uaa_clients_tcp_emitter_secret: (( merge ))
uaa_clients_tcp_router_secret: (( merge ))
uaa_database_password: (( merge ))
uaa_jwt_signing_key:
  private_key: (( merge ))
uaa_login_saml:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
uaa_ssl:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
cc_bridge_cc_uploader_server:
  ca: (( merge ))
  certificate: (( merge ))
  private_key: (( merge ))
